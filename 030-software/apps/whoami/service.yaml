---
apiVersion: v1
kind: Service
metadata:
  name: whoami
  labels:
    app: whoami
    vlan: lb
spec:
  selector:
    app: whoami
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
