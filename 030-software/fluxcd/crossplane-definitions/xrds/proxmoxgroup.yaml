apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: proxmoxgroups.platform.${publicDomain}
spec:
  scope: Namespaced
  group: platform.${publicDomain}
  names:
    kind: ProxmoxGroup
    plural: proxmoxgroups
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                comment:
                  type: string
                  description: Proxmox group comment
                group:
                  type: string
                  description: Proxmox group name
                propagate:
                  type: boolean
                  description: Proxmox propagate property
                roles:
                  type: array
                  items:
                    type: string
                  description: Proxmox role names to assign
              required:
                - group
            status:
              type: object
              properties:
                groupId:
                  type: string
                  description: The Proxmox group ID
