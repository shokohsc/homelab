apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: blocky
resources:
  - namespace.yaml
  - service.yaml
  - deployment.yaml
  - http-route.yaml
configMapGenerator:
- name: config
  files:
  - ./config/config.yml
generatorOptions:
  disableNameSuffixHash: true