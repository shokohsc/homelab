---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: backrest
spec:
  hostnames:
  - backrest.${domain}
  parentRefs:
  - name: cilium
    namespace: cilium
  rules:
  - backendRefs:
    - name: backrest
      port: 80
    matches:
    - path:
        type: PathPrefix
        value: /
