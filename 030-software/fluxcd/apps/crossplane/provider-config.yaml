---
apiVersion: proxmoxbpg.crossplane.io/v1beta1
kind: ProviderConfig
metadata:
  name: proxmox
spec:
  credentials:
    source: Secret
    secretRef:
      name: creds
      namespace: crossplane-system
      key: crossplane
