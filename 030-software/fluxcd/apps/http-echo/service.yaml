---
apiVersion: v1
kind: Service
metadata:
  name: http-echo
  labels:
    app.kubernetes.io/name: http-echo
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: http-echo
