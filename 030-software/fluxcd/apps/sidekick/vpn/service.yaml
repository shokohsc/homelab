---
apiVersion: v1
kind: Service
metadata:
  name: vpn
  labels:
    app: vpn
spec:
  type: ClusterIP
  ports:
    - port: 3030
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: vpn
