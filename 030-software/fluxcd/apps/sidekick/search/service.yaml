---
apiVersion: v1
kind: Service
metadata:
  name: search
  labels:
    app: search
spec:
  type: ClusterIP
  ports:
    - port: 3030
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: search
