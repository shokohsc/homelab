---
apiVersion: v1
kind: Service
metadata:
  name: cloudflare
  labels:
    app: cloudflare
spec:
  type: ClusterIP
  ports:
    - port: 3030
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: cloudflare
