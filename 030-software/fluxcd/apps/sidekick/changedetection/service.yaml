---
apiVersion: v1
kind: Service
metadata:
  name: changedetection
  labels:
    app: changedetection
spec:
  type: ClusterIP
  ports:
    - port: 3030
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: changedetection
