---
apiVersion: v1
kind: Service
metadata:
  name: kubernetesevents
  labels:
    app: kubernetesevents
spec:
  type: ClusterIP
  ports:
    - port: 3030
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: kubernetesevents
