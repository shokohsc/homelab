---
apiVersion: v1
kind: Service
metadata:
  name: email
  labels:
    app: email
spec:
  type: ClusterIP
  ports:
    - port: 3030
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: email
