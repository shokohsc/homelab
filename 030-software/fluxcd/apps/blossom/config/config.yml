publicDomain: "blossom.${domain}"

database:
  path: /app/data/sqlite.db

storage:
  backend: s3
  s3:
    endpoint: http://rustfs-svc.rustfs.svc.cluster.local:9000
    bucket: blossom
    accessKey: "${S3_ACCESS_KEY}"
    secretKey: "${S3_SECRET_KEY}"
    tmpDir: /tmp