---
apiVersion: v1
kind: Service
metadata:
  name: buzz
  labels:
    app: buzz
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
    - port: 9102
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: buzz
