---
apiVersion: v1
kind: Service
metadata:
  name: syslog
  annotations:
    lbipam.cilium.io/sharing-key: vip
spec:
  type: LoadBalancer
  ports:
  - port: 601
    targetPort: syslog
    protocol: TCP
    name: syslog
  selector:
    app.kubernetes.io/name: syslog
