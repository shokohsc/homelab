@version: 4.2
@include "scl.conf"

source s_network {
    network(
        transport("tcp")
        port(8601)
    );
};

destination d_stdout {
    file("/dev/stdout");
};

log {
    source(s_network);
    destination(d_stdout);
};
