apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: sonarr
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - http-route.yaml
  - secret.yaml
configMapGenerator:
- name: config
  env: ./config/sonarr.env
generatorOptions:
  disableNameSuffixHash: true