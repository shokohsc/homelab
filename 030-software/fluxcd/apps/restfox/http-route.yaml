---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: restfox
spec:
  hostnames:
  - restfox.${domain}
  parentRefs:
  - name: cilium
    namespace: cilium
  rules:
  - backendRefs:
    - name: restfox
      port: 80
    matches:
    - path:
        type: PathPrefix
        value: /
