---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: graphql
spec:
  hostnames:
  - apollo.comics.${domain}
  parentRefs:
  - name: comics
    kind: ListenerSet
    group: gateway.networking.k8s.io
    sectionName: apollo
  rules:
  - backendRefs:
    - name: graphql
      port: 80
    matches:
    - path:
        type: PathPrefix
        value: /
