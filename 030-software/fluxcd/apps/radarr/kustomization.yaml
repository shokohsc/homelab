apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: radarr
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - http-route.yaml
  - secret.yaml
configMapGenerator:
- name: config
  env: ./config/radarr.env
generatorOptions:
  disableNameSuffixHash: true