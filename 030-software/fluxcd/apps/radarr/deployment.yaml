apiVersion: apps/v1
kind: Deployment
metadata:
  name: radarr
  annotations:
    secret.reloader.stakater.com/reload: radarr
    configmap.reloader.stakater.com/reload: config
spec:
  selector:
    matchLabels:
      app: radarr
  template:
    metadata:
      labels:
        app: radarr
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
        fsGroupChangePolicy: OnRootMismatch
      containers:
        - name: radarr
          image: ghcr.io/home-operations/radarr:6.4.3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
          env:
            - name: TZ
              value: ${timezone}
            - name: RADARR__AUTH__APIKEY
              valueFrom:
                secretKeyRef:
                  key: api-key
                  name: radarr
            - name: RADARR__POSTGRES__USER
              valueFrom:
                secretKeyRef:
                  key: database-user
                  name: radarr
            - name: RADARR__POSTGRES__PASSWORD
              valueFrom:
                secretKeyRef:
                  key: database-password
                  name: radarr
          envFrom:
            - configMapRef:
                name: config
          ports:
            - name: http
              containerPort: 7878
              protocol: TCP
          startupProbe:
            tcpSocket:
              port: http
          livenessProbe:
            tcpSocket:
              port: http
          readinessProbe:
            httpGet:
              path: /ping
              port: http
          resources:
            limits:
              cpu: 1000m
              memory: 256M
            requests:
              cpu: 100m
              memory: 128M
          volumeMounts:
            - name: tmp
              mountPath: /config
            - name: 4to-lucio
              mountPath: /mnt/4to-lucio
            - name: 4to-sombra
              mountPath: /mnt/4to-sombra
            - name: 4to-zarya
              mountPath: /mnt/4to-zarya
            - name: downloads
              mountPath: /mnt/downloads
      volumes:
        - name: tmp
          emptyDir: {}
        - name: 4to-lucio
          nfs:
            server: nfs-4t-lucio.nfs-wd-lucio.svc.cluster.local
            path: /movies
        - name: 4to-zarya
          nfs:
            server: nfs-4t-zarya.nfs-wd-zarya.svc.cluster.local
            path: /movies
        - name: 4to-sombra
          nfs:
            server: nfs-4t-sombra.nfs-wd-sombra.svc.cluster.local
            path: /movies
        - name: downloads
          nfs:
            server: nfs-downloads.nfs-downloads.svc.cluster.local
            path: /
