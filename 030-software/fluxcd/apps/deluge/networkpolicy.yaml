---
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: deluge
spec:
  podSelector:
    matchLabels:
      app: deluge
  policyTypes:
    - Egress
  egress:
    - to:
      - namespaceSelector:
          matchLabels:
            namespace: vpn-egress-gateway
        podSelector:
          matchLabels:
            app: vpn-egress-gateway
