apiVersion: karpenter.sh/v1
kind: NodePool
metadata:
  name: vm-genji
spec:
  limits:
    cpu: "64"
    memory: 512Gi
  template:
    spec:
      nodeClassRef:
        group: karpenter.proxmox.sinextra.dev
        kind: ProxmoxNodeClass
        name: vm-genji
      # requirements:
      #   - key: "topology.kubernetes.io/zone"
      #     operator: In
      #     values: ["homelab/genji"]