---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: alloy
spec:
  hostnames:
  - alloy.${domain}
  parentRefs:
  - name: cilium
    namespace: cilium
  rules:
  - backendRefs:
    - name: alloy
      port: 12345
    matches:
    - path:
        type: PathPrefix
        value: /
