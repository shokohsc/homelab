---
apiVersion: v1
kind: Service
metadata:
  name: cnpg-platform
  labels:
    app: cnpg-platform
spec:
  selector:
    app: cnpg-platform
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
  type: ClusterIP
