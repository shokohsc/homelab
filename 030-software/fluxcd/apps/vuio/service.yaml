apiVersion: v1
kind: Service
metadata:
  name: vuio
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: http
  - name: dlna
    port: 1900
    protocol: UDP
    targetPort: dlna
  selector:
    app: vuio
  type: ClusterIP
