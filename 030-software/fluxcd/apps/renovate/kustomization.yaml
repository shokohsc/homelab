apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: renovate
resources:
  - namespace.yaml
  - cronjob.yaml
  - secret.yaml
configMapGenerator:
- name: config
  files:
  - ./config/config.js
generatorOptions:
  disableNameSuffixHash: true