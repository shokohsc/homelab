---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pyload
spec:
  hostnames:
  - pyload.${domain}
  parentRefs:
  - name: cilium
    namespace: cilium
  rules:
  - backendRefs:
    - name: pyload
      port: 80
    matches:
    - path:
        type: PathPrefix
        value: /
